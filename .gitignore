# Sensitive data files — never commit these
*.xlsx
*.xls
*.csv

# One-off data backfill / cleanup SQL scripts — real client data, run manually
# via Supabase SQL Editor, never committed
/local-sql/

# Environment / secrets
.env
.env.*
*.secret

# OS
.DS_Store
Thumbs.db
